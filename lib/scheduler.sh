# TODO: Based on the config file in tmp folder, it will generate a spool file
# for each service, which contains the last check timestamp, or if there is an
# action was done, or needs to be done it will also make a note there

scheduler() {
  echo "Running..."
}
